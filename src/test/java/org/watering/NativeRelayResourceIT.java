package org.watering;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeRelayResourceIT extends RelayResourceTest {

    // Execute the same tests but in native mode.
}